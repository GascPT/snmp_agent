module snmp_agent {

    yang-version "1";

    // namespace
    namespace "urn:srl_test/snmp-agent";

    prefix "snmp_agent";

    revision "2022-03-14" {
        description "Initial revision";
        reference "TBD";
    }


    grouping snmp_agent-top {
        description "Top level grouping for fib_agent sample app";
        
        leaf-list targets {
            description "Endereço de destino das traps snmp";
            type string;
            
        }

        container elements {
            //presence "presence container";
            description "Top level enclosing container for fib_agent sample app 
                         config and operational state data";

            
        }
    }

    // data definition statements
    uses snmp_agent-top;
}
